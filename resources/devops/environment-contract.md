# Environment Contract — [Project Name]
**Version:** [N] | **Date:** [date]

## Environment Variables

| Name | Purpose | Required in | Notes |
|---|---|---|---|
| [VAR_NAME] | [What it configures — one sentence] | [dev / staging / prod / all] | [Optional: format constraints, example shape without real value] |

## Configuration Notes
[Any patterns the Builder must follow — e.g. "all secrets are injected at runtime via the platform secret manager, never read from .env files in production"]
