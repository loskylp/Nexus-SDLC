Stage all modified and new files relevant to the current work (exclude .claude/agent-memory/, memory/, and any files that look like secrets or scratch), write a concise but descriptive commit message that summarises what changed and why, commit, then push to origin.

Do not add Co-Authored-By to the commit message.
